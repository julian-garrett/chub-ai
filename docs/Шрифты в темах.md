# 🔤 Шрифты в темах

## 📝 Переменные для шрифтов

В системе есть несколько переменных для управления шрифтами:

```css
:root {
  /* Основной шрифт для текста */
  --font-primary: 'Crimson Text', 'PT Serif', Georgia, serif;
  
  /* Шрифт для всех заголовков (h2-h6) */
  --font-heading: 'Cinzel', 'Philosopher', serif;
  
  /* Шрифт для заголовка бота (h1) - можно переопределить отдельно! */
  --font-h1: var(--font-heading);        /* По умолчанию = font-heading */
  
  /* Моноширинный шрифт для кода */
  --font-mono: 'Courier New', monospace;
}
```

## 🎯 Шрифт заголовка бота

**Да, можно задавать отдельно!** Используйте переменную `--font-h1`:

```css
:root {
  /* Переопределяем только шрифт для h1 */
  --font-h1: 'Permanent Marker', cursive;
  
  /* Остальные заголовки останутся с font-heading */
  --font-heading: 'Cinzel', serif;
}
```

## 📚 Примеры использования

### 1. Рукописный шрифт для заголовка

```css
@import url('https://fonts.googleapis.com/css2?family=Caveat:wght@500;700&display=swap');

:root {
  --font-h1: 'Caveat', cursive;
  --font-heading: 'Cinzel', serif;  /* Остальные заголовки */
}
```

### 2. Маркерный шрифт (для блогов)

```css
@import url('https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap');

:root {
  --font-h1: 'Permanent Marker', cursive;
  --font-heading: 'Crimson Text', serif;
}
```

### 3. Современный sans-serif

```css
:root {
  --font-h1: 'Segoe UI', 'Roboto', sans-serif;
  --font-heading: 'Cinzel', serif;
}
```

### 4. Готический шрифт

```css
@import url('https://fonts.googleapis.com/css2?family=UnifrakturMaguntia&display=swap');

:root {
  --font-h1: 'UnifrakturMaguntia', cursive;
  --font-heading: 'Cinzel', serif;
}
```

## 🔗 Где используется каждый шрифт

| Переменная | Используется для |
|------------|------------------|
| `--font-primary` | Основной текст (p, li, td) |
| `--font-heading` | Заголовки h2, h3, h4, h5, h6 |
| `--font-h1` | **Заголовок бота: `h1` и `h3.ant-typography`** |
| `--font-mono` | Блоки кода (`code`) |

## ⚠️ Важно про `h3.ant-typography`

В chub.ai заголовок бота - это **`<h3 class="ant-typography">`**, а не обычный `h1`!

**Хорошая новость:** Переменная `--font-h1` применяется к обоим:
- ✅ `h1` (если используется)
- ✅ `h3.ant-typography` (реальный заголовок бота)

**Также применяются все эффекты:**
- ✅ Анимации (`--animation-h1`)
- ✅ Свечение (`--glow-h1`)
- ✅ Тени текста (`--text-shadow-h1`)
- ✅ Трансформации (`--text-transform-h1`)
- ✅ Псевдоэлементы (`--h1-before-content`)

## ⚠️ Важно

1. **Не забудьте импортировать шрифт**, если используете Google Fonts:
   ```css
   @import url('https://fonts.googleapis.com/css2?family=Название+Шрифта&display=swap');
   ```

2. **Всегда указывайте fallback шрифты**:
   ```css
   --font-h1: 'Caveat', cursive, serif;
   ```

3. **Шрифт применяется автоматически** - не нужно писать дополнительный CSS!

## 💡 Пример полной темы

```css
@import url('https://fonts.googleapis.com/css2?family=Caveat:wght@500;700&family=Cinzel:wght@400;600&display=swap');
@import url('base-improved.css');

:root {
  /* Цвета */
  --color-primary: #8b4513;
  --color-secondary: #d4af37;
  /* ... */
  
  /* Шрифты */
  --font-primary: 'Crimson Text', serif;
  --font-heading: 'Cinzel', serif;
  --font-h1: 'Caveat', cursive;        /* Рукописный для заголовка бота */
  --font-mono: 'Courier New', monospace;
}
```

**Результат:** Заголовок бота будет с рукописным шрифтом, а остальные заголовки - с готическим! 🎨

